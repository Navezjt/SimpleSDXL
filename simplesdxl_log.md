# 2024-09-16
* 解锁Flux的lora和可图lora。
* 自动修图开关移到增强修图标签内。修图更多情况是针对已有图片，而新生成图片往往并不知道是否需要修。
* 添加提示面板开关，可点击触发批量通配符面板。
* 反推提示词和图片参数提取两个标签移入参数设置栏，避免被锁在图像输入里。
* 添加基于预置包的基础模型过滤功能，避免预置包配置和模型不匹配情况发生。
* 修复Comfyd引擎的跳过和中断逻辑bug，使其效果与Fooocus引擎保持一致。
* 优化预置包参数和预置包导航。Flux可适用所有Flux模型并自动适配硬件环境。Flux🚀优选Hyp8Q5KM模型，在低配环境下具有速度与质量的平衡，同时也支持Flux Lora。可图🚀参数优化提升出图质量。新增两个平铺预置包，适用无缝贴图场景。
* 升级comfyd到最新版。优化下载安装和启动流程，强制基础包检测，并提供模型包安装脚本。拆分基础包缩小到25G，并按模型种类进行独立分包。
* 特性增强: 已出图历史索引增加数量统计显示，增加可显示总目录量参数，避免屏幕无限拉长。
* 交互优化: 自定义宽高移到图片尺寸下，采样步数移到高级选项的顶部，图片提示放第一位，放大和变化的强度以及重绘强度和羽化系数设置提到场景里，右侧栏滚动与左侧主栏解耦独立滚动，通配符上词自动去除通配符标签。
* 适配优化: RAM低于32G则Flux-dev的dtype自动适配fp8。
- 47DEQpj8HB|gNWoFg2h6M

# 2024-08-20
* 新架构进一步优化，提升在Windows环境的兼容性，压缩Fooocus和Comfy后端切换的资源消耗。
* 优化支持最低6G显存的Flux模型出图，提供质量优先的Fluxdev和速度优先的Flux+两个预置包，并可根据系统资源自动适配。
* 同步升级主线v2.5.5，优化增强修图UI，更符合Fooocus交互习惯。
- 57juLRM|QUNVCnC|OK9Eu8Kxr_

# 2024-07-31
* 优化了新架构，更稳定，更快速。
* 新增可图模型的支持。
* 并合并主线2.5.2版本，并对自动修图进行UI优化。

# 2024-06-30
* 架构升级到SimpleSDXL2，新增Comfy和Diffusers后端，支持国产混元HyDiT模型，SD3m模型，最低6G可用。
* 新增融图打光模块，可自主生成前景及蒙版，可自动抠取产品或人物图片切换场景进行融合。
* 升级OBP一键提示词到最新版，新增随机提示词预置分类
* UI整体优化。

# 2024-05-28
* 合并主线v2.4.1版本。
* 修订若干bug。
* 为新大版本升级扫清障碍，建立基线。

# 2024-04-23
* 新增Superprompt超级提示词扩展，为提示词增加细节描述。升级OneButtonPrompt组件到最新版。
* 新增SD3生图引擎接口，可以无缝对接新引擎生成图片。
* 将OBP、Superprompt和翻译入口集成到提示词组件，方便快捷操作，聚焦提示词的优化。
* 优化界面，包括预置包导航浮层提示、提示词框token数统计、图生图多个参数前置到操作页面等。

# 2024-03-26
* 集成OneButtonPrompt组件，可以根据预设的内容场景，随机出一键组合提示词，是强大的提示词组织工具。
* 修订通配符处理逻辑，完全兼容主线版本逻辑，多通配符随机抽取。
* 增加预置包模型文件下载提示，避免初始状态频繁切换预置包带来的困扰。
* 修订嵌参图片参数提取流程，避免无参图片带来的错误。
* 合并主线2.3.0，同时修订部分bug。

# 2024-03-09
* 新增lightning出图模式，自动下载和加载加速模型`sdxl_lightning_4step_lora.safetensors`。
* 优化增强通配符模块，通配符可嵌套，可动态加载。
* 与主线2.2.1版本合版，新增LoRA取值范围可定制，支持png/jpg/webp图片格式，嵌参图片信息可与Civitai兼容等主线带入功能。
* 整合新的嵌参和提参模块，保障前后版本一致性和可用性。调整UI，取消冗余选项，将预置包生成入口调入"增强"Tab。
* 梳理参数流程，预置包新增FreeU和翻译器配置等9项预置参数，具体见preset/readme.md

# 2024-02-18
* 新增通配符和批量提示词工具，提示词支持[]形式的词组，在同一seed下从词组中抽词进行组合批量生成图片，词组支持通配符，可以随机抽，也可以指定位置顺序抽。提示词框增加通配符辅助输入工具，可识别输入内容自动弹出，可展示和点击输入所有通配符及其包含的词组。从OneButtonPrompt同步20+通配符内容集，并进行了裁剪适配。
* 新增指定Fooocus配置文件的启动参数`--config`，与Fooocus共享模型配置文件，实现升级包同样的功能，取代原升级包功能。
* 新增启动参数`--dev`，可以快捷启动SimpleSDXL的开发版，参与新功能的体验和反馈。
* 修复主线重绘中refiner切换和step计算错误，by xhoxye。

# 2024-01-31
* 新增智能抠图，基于Bert+Sam和U2net实现在语义理解上图片分割，生成蒙板后进行重绘。
* 新增翻译小模型，降低本地翻译的算力要求。
* 升级默认预置包主模型到v8版，添加了Turbo模型预置包（加速）、albedobaseXL模型预置包（融合）和社群活跃用户LEO贡献的DragonYear预置包（龙年贺春），内含龙年拜年定制风格。
* 值此新春之际，用全新的SimpleSDX贺春版感谢朋友们的厚爱。祝大家新春快乐，龙年腾达！

# 2024-01-22
* 已出图集索引优化，支持手工在目录内批量删除图片而不影响索引关系。
* 手机使用界面优化，导航和图片索引适配手机竖屏。
* 更新预置包说明文件，展示可支持的配置参数，见 `/presets/readme.md`。
* 修复Seed参数和启动cookie识别等bug。

# 2024-01-20
* 预置包、嵌参图片和提参重生进行接口整合优化，扩充可预置和嵌参的范围，支持Turbo类模型和自定义风格样式的嵌参和预置。
* 预置包导航重构，用Gradio内置组件替换JS外挂组件，实现session based的预置包加载，加载预置包不需要浏览器级的重载了，更轻量更快速。
* 背景主题设置移入"增强"菜单内，新增语言设置，可通过界面切换语言。
* 嵌参和预置包生成时自动获取模型MUID，提高嵌参和预置对外交换的可用性。

# 2024-01-16
* 嵌参图片上线，可以生成嵌参图片，可以上传识别嵌参图片。从参数查看到提参重生，从预置包导航到预置包生成，最后到嵌参图片，这一些列简化参数配置和使用的功能终于闭环了。
* 启动流程优化，平行升级版支持自动获取主线配置，免配置模型目录和出图目录。新增国内模型下载源。
* 界面文字优化，图片工具改名为参数工具，描述图片改名为图片提参，增加图片宽高比的具体场景描述，图片重绘默认激活蒙板上传等。
* 混编翻译器增加不翻译选项。

# 2024-01-08
* 图片工具：新增浏览历史图片自动回填提示词功能，默认关闭，需在”增强“菜单主动打开开关。
* 图片工具：新增图片删除功能，联动删除出坏的图片和日志记录。
* 混编翻译器：为在线翻译器增加环境变量，避免启动时定位不到区域引起的故障。
* Bug修复：修订主线引入新安装流程cv2模块缺失bug。同步主线版本v2.1.861


# 2024-01-03
* 启用访问位置识别，根据用户位置选择下载模式。增加启动参数`--location`，可以强制指定启动位置，国内用"CN"，国外用"EN"。
* 默认用局域网IP启动，在"增强"菜单显示手机访问局域网地址，方便用手机访问。

# 2024-01-01
* 界面交互优化，状态信息迁移到会话(session)，多浏览器访问互不干扰。
* 优化了手机访问界面，PC和手机可同步使用。
* 生成图片过程中禁止预置包切换，保障出图过程的完整。
* 增加相册缓存，提升历史图片索引切换速度。
* 修复模型路径bug，同步主线版本v2.1.859。

# 2023-12-30
* 提示词翻译器升级：支持中英文混合编辑，支持翻译后再编辑，可自主选择本地模型翻译还是第三方接口翻译.
* 启用SimpleSDXL独立版本号："v发布时间.版本哈希"
  
# 2023-12-28 
* 预置包生成流程和加载流程优化，启用MUID模型文件标识，文件名不同自动转换，本地无模型文件自动下载，自动识别embeddings模型。
* 模型MUID标识远程同步优化，与启动流程解耦，由手动触发。
* 预置包导航视觉优化，采用Fooocus原生色系。
* 页面顶部新增日志消息提示层，提取和展示Fooocus原版和SimpleSDXL版的最新更新日志。

# 2023-12-22
* 修订启动流程bug，优化Windows启动包和升级包。一键启动包瘦身到75M。

# 2023-12-20
* Initial Update Log Version.
